apiVersion: v1
kind: Service
metadata:
  name: festival-service
spec:
  selector:
    app: festival-service
  ports:
    - protocol: TCP
      port: {{ .Values.port }}
      targetPort: {{ .Values.port }}
  type: NodePort